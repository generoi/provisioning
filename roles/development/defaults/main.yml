---
development:
  vim:
    install: true

  nodejs:
    install: true
    packages:
      - grunt-cli
      - yslow
      - bower
      - browserstack-cli
      - jshint
      - marked
      - yo
      - generator-webapp
      - phantomjs

  ruby:
    install: true
    gems:
      - compass
